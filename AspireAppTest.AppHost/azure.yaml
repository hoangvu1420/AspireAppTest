# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: aspireapptest-apphost
parameters:
  postgresUser:
    secureValue: 
  postgresPassword:
    secureValue: 
services:  
  app:
    language: dotnet
    project: ./AspireAppTest.AppHost.csproj
    host: containerapp
